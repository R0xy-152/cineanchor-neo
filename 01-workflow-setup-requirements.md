# 协作工作流需求 — 当前可执行项

> 作者：Andy（对话 agent）· 状态：草稿，待启鸣确认
> 范围：单人开发 + 双 agent 工作流搭建。
> 来源：对《如何搭建一个端到端业务需求专家 Agent》的拆解，按单人开发场景过滤。

本文档列出归类为「现在就能做」的事项。搁置/不适用项列在文末，以明确边界。

> 关于 Spike 规则：AGENTS.md 当前不建议在 Spike 阶段创建 docs/ 目录树。
> 启鸣已明确选择现在就纳入知识文档（scope / codemap / ADR），保持轻量、置于 docs/ 下，与一次性 Spike 代码分离。
>
> 关于安全项：启鸣已决定（2026-06-27）忽略 Phase 0 安全护栏，原 R2（main 分支保护）、R3（token 收窄）移至文末「搁置」区，编号保留以维持跨文档引用一致。

---

## R1. 双 agent 角色划分（纯流程，无代码）

- **Andy（对话）** = 上游产品大脑：需求收集、澄清、决策、验收标准、代码/计划评审。
- **Claude Code（本地）** = 执行者：读取 `.claude/CLAUDE.md` + `AGENTS.md`，运行 plan mode，编写代码，本地渲染视频。
- **启鸣** = 每个关卡的唯一审批人。
- **为什么现在做：** 零代码、零基建，是其他一切的骨架。
- **交付物：** 在 `.claude/CLAUDE.md` 追加一段角色划分说明（启鸣批准后由 CC 完成）。

## R4. 需求/计划交接协议（本文档即首个实例）

- **流程：** Andy 编写 markdown 文档（需求/验收标准）→ push 到 `Andy` 分支 → 启鸣 review → 启鸣决定是否交给 Claude Code。
- **规则：** Andy 只写 `Andy` 分支，绝不写 `main`、`spike/*`、`feature/*`。
- **为什么现在做：** 轻量、异步、留下可评审的痕迹。无需 CI、无需 CODEOWNERS（②③ 层已舍弃）。
- **交付物：** 干净 `Andy` 分支上的本批文档。

## R5. plan mode + 双重评审关卡（执行前）

- **流程：** Claude Code 读取已批准需求 → plan mode 产出计划 → **启鸣 review** + **Andy review** → 双方通过后 CC 才执行。
- **为什么现在做：** 纯流程。在写代码前拦住范围蔓延。
- **说明：** 保持轻量 — Spike 阶段计划可以是几条要点，而非完整设计文档。

## R6. 视频验收闭环（人在环中，强制）

- **规则：** 任何会产出视频的需求闭环，必须由 Claude Code 本地渲染，并输出**测试视频文件路径**供启鸣评审。Andy 无法渲染（沙箱无 Blender/FFmpeg）。
- **为什么现在做：** 产品最终产出是视频，只有人能判断好坏。已批准。

## R7. 轻量 pre-push 冒烟检查（可选，轻量级）

- **动作：** 一个最小 pre-push hook，跑现有的冒烟检查（如「渲染产出 N 帧、无黑帧、MP4 可播放」）。**不是**完整 TDD 硬关卡。
- **为什么现在做但保持轻：** 当前代码仍在演进。进入 V0.1 时再升级为硬关卡。

## R8. 系统边界文档 — `docs/scope.md`（系统边界）

- **动作：** 把「做什么 / 不做什么」边界抽取整合进 `docs/scope.md`：CineAnchor 做什么（PNG/GLB → 模板 → 1080p MP4），以及明确的非目标（无预览一致性、无自动 BGM、无云/SaaS、无账号系统、ComfyUI 仅保守增强、串行渲染、纯本地）。
- **来源：** 从 `AGENTS.md`「Non-Goals Before V1.0」+ `.claude/CLAUDE.md` 约束提炼为单一权威参考。
- **为什么现在做：** 防止 Andy 和 Claude Code「好心」加超范围功能。对单人开发价值最高的知识文档。

## R9. 代码地图 — `docs/codemap.md`

- **动作：** 一页式模块地图：每个重要文件/目录的职责与调用关系（如 web 入口 → FastAPI 路由 → Blender 渲染脚本 → FFmpeg → MP4）。
- **为什么现在做：** 让 Andy 和 CC 不必每次重读整棵树就能定位代码。
- **说明：** 保持浅层、改代码时顺手更新；CC 触碰代码时可自动维护。

## R10. ADR — `docs/adr/`（架构决策记录）

- **动作：** 把技术选型决策记为编号 ADR（状态：proposed / accepted / superseded）。每条 ADR = 背景、决策、备选方案、后果。
- **已知现状：** 启鸣本地已有 `docs/adr/`，含 `ADR-004-no-full-frame-ai-video-regeneration.md`。**先对齐现有集合再写**，避免分叉。
- **为什么现在做：** 技术选型期最合适的持久知识形态 — 记录「为什么」选某路线，又不锁死代码。

---

## 搁置 / 暂不做（明确边界）

- **R2. main 分支保护** — 启鸣决定忽略 Phase 0 安全护栏（2026-06-27），暂不启用。
- **R3. token 收窄** — 同上，暂不强制 token 收窄/轮换。
- **历史口径** — 尚无历史积累，无可对齐口径。搁置。
- **单独的 LLM Wiki** — 单人项目用 `.claude/CLAUDE.md` + ADR 已能承担此角色，不另建 wiki。
- **路径白名单 CI（② 层）/ CODEOWNERS（③ 层）** — 主动舍弃。残留敞口：token 可写非 main 分支内容；因已忽略 main 保护，残留风险由启鸣知悉并接受。

---

## 待确认

1. 「现在就能做」清单是否正确、完整？
2. 是否确认 R2/R3 安全项搁置、接受相应残留敞口？
3. R10 ADR — 本地结构已提供，是否同意按 `docs/adr/` 现有集合对齐续写？
4. Andy 是否继续起草 `.claude/CLAUDE.md` 角色划分段落（R1）？

本文件仅存在于干净的 `Andy` 分支（orphan，无 `main` 文件）。未写入 `main`。
