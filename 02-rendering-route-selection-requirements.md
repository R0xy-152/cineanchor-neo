# 渲染技术路线选型 — 需求与推荐

> 作者：Andy（对话 agent）· 状态：已通过（启鸣预批准，免 review，2026-06-27）
> 输入：`attachments/deep-research-report (1).md`（3D 资产深度引导与多视图条件可控运镜视频管线可行性评估）
> 决策性质：产品决策（核心渲染路线选型）→ 触发 ADR（见 `docs/adr/ADR-005-rendering-route-selection.md`）

---

## 目标与评判标准

**目标：** 以「最快得到可放进作品集的视频展示」+「最好的视频效果」为双标准，确定 CineAnchor 的实现技术路线。

**「视频好」的硬标准（启鸣定义）：**
1. 画面可控
2. 视频画面不会扭曲
3. 有逻辑
4. 帧间一致

---

## 核心判断

**这四条标准本质上是「确定性渲染」的主场，不是「生成式 AI」的主场。**

- 确定性 3D 渲染（Blender）在这四项上**天然满分**：每帧都是同一份几何沿相机轨迹投影出来的，不存在「脑补」，所以不会扭曲、帧间天然一致、画面完全可控。
- 生成式 AI 视频恰恰在这四项上**引入风险**：闪烁、主体形变、身份漂移。调研报告自身结论即「不应以任何单一现成商用 API 作为主方案」；项目既有 `ADR-004` 也已否决全帧 AI 重绘。

**结论：** 按启鸣的标准，「最快」与「最好效果」在此**不冲突** —— 确定性路线同时拿下两者，唯一牺牲的是「生成式 AI 电影感」这一上限，而该上限可由商用 API 作为**非核心润色层**补足。

---

## 技术路线（按推荐程度排序）

### ① 最推荐 —— Blender 确定性渲染 + 后期润色（FFmpeg / LUT / 合成 / 粒子 / 运动模糊）

- **是什么：** 即 v0.1 已验证链路（Web → FastAPI → Blender Eevee → FFmpeg → MP4）+ dolly-ember 等增强。
- **四条标准：** 全部满分（确定性渲染）。
- **进作品集速度：** 最快 —— 大部分链路已建好。
- **效果上限：** 干净、专业的 3D 游戏宣传片观感。
- **定位：** **主线，现在就做。** 作品集要讲的故事「我做了一个可控的游戏宣传片生成器」，这条链路本身就是产品故事。

### ② 润色层（叠加在①之上，非独立主线）—— 商用 API（Veo 3.1 / Sora / Runway）做观感上限对照

- **是什么：** 用①产出的**结构正确**关键帧，喂给商用 API 追求观感质量。Veo 支持最多 3 张参考图 ≈ 你的三视图。
- **优点：** 出「好看」画面最快、零基建。
- **缺点：** **不可控** —— 公开接口不接受深度图/相机矩阵，运镜只能靠 prompt 文字，主体可能漂移 → 在「可控 / 不扭曲」上不达标。
- **定位：** 仅做局部「wow 镜头」或观感对照组，**绝不进核心控制逻辑**。

### ③ 条件执行 —— 几何先行混合（3DGS / 多视图 → 粗渲染 → 仅对静帧做保守 AI 增强）

- **是什么：** 报告中 ViewCrafter / MVGD / 3DGS 路线，稳定性高于纯深度直控；符合 ADR-004「AI 只做静帧色彩/风格参考」。
- **优点：** 抬高效果上限且相对稳。
- **缺点：** 需 GPU、研究级整合、慢。
- **定位：** **等①跑顺、需进一步提画质时再启用。**

### ④ 暂不做 —— 控制先行 AI 视频扩散（depth 序列 + 相机轨迹 → Control-A-Video / CameraCtrl / CamI2V）

- **是什么：** 报告主探索方向，效果上限可能最高。
- **缺点：** 研究级、256×256 / 16 帧、闪烁 + 形变风险、首版 3–7 天、量产 2–4 周。
- **定位：** 直接冲突「最快进作品集」，且正面威胁四条标准。**现阶段不作为主线**，记录在 ADR 作「已评估、暂缓」。

---

## 落地建议

- **主线走①** —— 立即能产出作品集级视频。
- **想要个别惊艳镜头时用②** —— 对单个关键帧润色，不进核心逻辑。
- **③④ 写进 ADR** 作为「已评估、暂缓」的决策记录，主线稳定后按需启用。

---

## 后续动作

1. 本决策已记录为 `docs/adr/ADR-005-rendering-route-selection.md`（Accepted）。
2. 下一需求闭环聚焦①主线：在 v0.1 基础上推进 dolly-ember 等增强，产出作品集视频。
3. ②商用 API 对照、③几何先行混合，留待主线达标后按需立项。
