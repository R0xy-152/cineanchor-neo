# 粒子与背景实现方案 — 需求（具体操作）

> 作者：Andy（对话 agent）· 状态：草稿，待启鸣 review
> 背景：路线①（Blender 确定性渲染 + 后期润色）下，运镜验收已通过并锁定；粒子在主管线内始终难调；黑布背景过死。
> 决策性质：方法层产品决策（粒子/背景实现方式）→ review 通过后将记为 `ADR-006`。

---

## 一、问题定位

1. **粒子难调的真正原因 ≠ 工具选错，而是耦合度太高。** 粒子与 3D 主场景、运动模糊、相机一起渲染，变量纠缠：改一个粒子参数要重渲整条管线才能看到结果，迭代以分钟计。
2. **不能用 AI 做粒子。** 四条标准里最致命的是「帧间一致」。粒子=小+亮+高速运动，正是生成式 AI 最不稳的元素，每帧重生成会让小亮点位置/数量/亮度抖动→闪烁。用 AI 救粒子=用最在意的指标换一个本可确定性锁死的东西。
3. **背景「过死」是因为纯黑太平**，这是最容易确定性解决的问题，同样不需要 AI。

**总原则：粒子与背景都走确定性合成，AI 不介入这两处**（AI 仍按 ADR-005 留在「个别 wow 镜头观感对照」位置）。

---

## 二、粒子方案 —— 解耦为独立图层

### 方案 A（推荐，最快进作品集）：2D 火星叠加层

把火星做成一个独立的、透明背景的循环层，后期用合成叠加到主画面上。3D 主渲染**完全不含粒子**。

**具体操作：**

1. **素材来源（二选一）：**
   - A1. 用现成 ember/spark 库存素材（透明通道或黑底视频）；
   - A2. 用 Blender 单独渲一个「纯粒子层」：黑底或透明，**不含枪械、不含背景**，只有火星 + 运动模糊。一次渲好可反复复用。
2. **取色：** 火星颜色沿用既定「与枪械风格色彩一致」逻辑，从主体素材自动取主色（沿用 dolly-ember 已定的取色方式），作为粒子发光色参数。
3. **后期合成（FFmpeg）：** 用 `screen` 或 `add` 混合模式把火星层叠到主渲染之上。
   - 黑底素材 → `blend=all_mode=screen`（黑色自动透明）；
   - 透明通道素材 → `overlay` 直接叠。
4. **可调参数（不重渲 3D，秒级迭代）：** 叠加层不透明度、数量（用多/少层叠加控制密度）、整体色调、运动速度（变速播放叠加层）。

**优点：** 完全确定性、帧间天然一致、调试秒级（改叠加层完全不动 3D）。

### 方案 B（次选）：Blender 粒子单独渲为 pass

主体一份、粒子一份，分开渲、后期合成。保留 Blender 物理真实感，但比 A 慢、仍需调物理参数。**仅当 A 的观感不够时再上。**

**判定条件：** 先做 A；若 3 次以内的库存/单层素材都达不到「火星拖影成光条」的真实感，再切 B。

---

## 三、背景方案 —— 确定性增强（不用 AI）

黑布过死的根因是太平。按以下叠加，全部是 Blender 灯光或 FFmpeg 后期一两步：

1. **微弱径向渐变 + vignette**：中心略亮、四周压暗 → 立刻有空间感。
   - 后期实现：FFmpeg `vignette` 滤镜；或 Blender 世界/合成节点加径向渐变。
2. **Rim light / 边缘光**：一道边缘光打在枪械轮廓上 → 主体从背景跳出。
   - Blender 实现：在主体侧后方加一盏聚光/区域光，仅照亮轮廓。
3. **极淡布料纹理或噪点**：背景不再是纯黑死面。
   - 实现：背景平面贴极淡布纹理；或后期加极轻 `noise`。
4. **可选 bokeh 光斑**：背景几颗缓慢虚焦光斑，做成 2D 叠加层（同粒子层思路）。

**约束：** 背景增强不得喧宾夺主——主体始终是枪械，背景只提供层次与空间感。

---

## 四、验收标准

| # | 标准 | 判定方式 |
|---|------|---------|
| 1 | 运镜不变 | 与已通过版本逐帧一致（背景/粒子改动不得影响相机轨迹） |
| 2 | 帧间一致 | 粒子无逐帧闪烁/跳变；连续播放无抖动 |
| 3 | 不扭曲 | 枪械主体几何零变化（确定性渲染保证） |
| 4 | 火星观感 | 「小而少、快速运动、拖影成光条」三特征齐备 |
| 5 | 颜色一致 | 火星主色与枪械风格色一致（自动取色） |
| 6 | 背景层次 | 黑布不再死平：有渐变/vignette/rim light 带来的空间感，且不喧宾夺主 |
| 7 | 可调试性 | 调粒子密度/颜色/速度**无需重渲 3D 主场景**（叠加层方案达成） |
| 8 | 本地产物 | Claude Code 本地渲染，输出测试视频文件路径供启鸣人工验收（按规则 R6） |

---

## 五、落地顺序

1. 先做**背景确定性增强**（改动小、风险低、立竿见影）。
2. 再做**粒子方案 A（2D 叠加层）**，解耦后秒级调火星观感与颜色。
3. A 观感不达标才切**方案 B**。
4. 全程不引入 AI。

---

## 六、待启鸣确认

1. 粒子是否同意「方案 A（2D 叠加层）优先、A 不达标再切 B」？
2. 背景四项增强（渐变+vignette / rim light / 纹理噪点 / 可选 bokeh）是否全要，还是先做前两项？
3. 确认通过后，Andy 将本决策记为 `ADR-006`（粒子与背景：确定性合成优先、否决 AI），并交由 Claude Code 进入 plan mode 执行。
