# 粒子可见性与观感纠偏 — 需求（具体操作）

> 作者：Andy（对话 agent）· 状态：草稿，待启鸣 review
> 触发：loop 03 首次产出经启鸣人工验收**未通过**。CC 报告 58/58 测试通过、视频「成功生成」，但真人观看发现核心可视产物缺失。
> 关系：本 loop 是对 `ADR-006` / `03-*` 的纠偏闭环，不改变 ADR-006 的方法决策（确定性合成、否决 AI 不变），只修「做出来但看不到/不对」的执行结果。

---

## 一、本轮要修的两个缺陷（来自真人验收）

**缺陷 1（致命）—— 火星叠加层是纯黑的，看不到火星。**
- 现象：`assets/overlays/embers_default.mp4` 单独播放是纯黑屏，没有任何火星。
- 后果：用 `screen` 混合一个全黑层 = 等于什么都没叠。启鸣最在意的「火星效果」实际为零。
- 暴露的流程漏洞：**自动测试 58/58 全过、CC 报「成功生成」，但叠加层资产是空的**——测试只验了代码路径，没验「这个层里到底有没有可见的火星」。黑层被静默合成、被当成成功。

**缺陷 2 —— 最终视频像加了滤镜，颜色与主体接近，火星没跳出来。**
- 现象：`particle_final.mp4` 看起来是「主体被加了一层滤镜」，整体颜色和主体相近。
- 根因推断：因为火星没显示，画面里实际只剩 bokeh + 暗角 + 自动取色的色调；自动取色逻辑把整体压成与主体同色调的一层，结果像滤镜而非「飞溅的亮火星」。
- 产品层启示：`ADR-006` 验收 #5「火星主色与枪械风格色一致」被**过度执行**成了「整体同色 → 糊成滤镜」。「一致」不等于「同色/被洗掉」。

---

## 二、本轮目标（要达成什么，不是怎么调参）

1. **火星层独立可见**：`embers_default.mp4` 单独播放，必须能清楚看到「小而少、快速运动、拖影成光条」的火星亮点（即 03 验收 #4 三特征），而不是黑屏。
2. **堵住「空层被当成功」的漏洞**：叠加层生成后必须有一道**自动的非黑/亮度校验**，黑层或几乎全黑的层要让生成步骤**失败报错**，而不是静默通过。
3. **火星在最终合成里跳出来**：`particle_final.mp4` 必须读作「离散的明亮火星在飞」，而不是「主体被加了一层滤镜」。火星亮度/对比要明显高于主体与背景，作为光点存在。
4. **修正取色规则**：火星沿用枪械的**色系（hue family）**，但必须高亮度/高饱和、与主体形成对比，**不得塌缩成与主体同一片平色**。色彩一致 ≠ 颜色相同 ≠ 整体被染成一层。
5. **背景效果不得喧宾夺主**：当前画面里 bokeh + 暗角成了主导（因为火星缺席）。修好火星后，火星是主要可见粒子效果，bokeh/暗角/边缘光退居其后（守 `ADR-006` 「不喧宾夺主」约束）。

---

## 三、具体操作要求

1. **先诊断火星层为何全黑**（CC 在 plan/执行里查 `generate_overlays.py`）：常见根因——发射强度/曝光太低、粒子太小太少、世界背景吞掉了亮点、或渲染设置让发光不可见。先把**单独的火星层**调到肉眼明确可见，再谈合成。
2. **加自动非黑校验**：对生成出的 `embers_default.mp4` / `bokeh_default.mp4` 做亮度自检（如最大/平均亮度高于阈值、或亮像素数量达标），不达标 → 生成失败、明确报错。把这道校验纳入测试或生成脚本，使「黑层」不可能再被当成功。
3. **重平衡合成**：火星层亮度/对比拉到主体之上；取色改为「同色系 + 提亮提饱和」，避免整体同色。bokeh/暗角强度下调到从属位置。
4. **A→B 升级门控要变成真的**：CC 自报当前门控是「占位符、永远通过」。本轮**先修方案 A 的可见性**（黑屏是生成 bug，不是 A 方法本身失败，先修 A，不要因此跳 B）；同时把门控做成**真实的质量判定**（哪怕是人在环里看一眼），不能再是永远 pass 的桩。只有当 A 修好、火星可见但**仍达不到「拖影成光条」真实感**时，才按门控切方案 B。
5. **顺带处理 CC 自报的已知风险**（不作为本轮验收重点，但需解决或明确记录）：
   - schema 重大变更（`particles` 由 bool 改为 spec）会让旧 payload 失效 → 至少更新 720p 烟雾测试预设，使其适配新 schema；
   - `compose_mp4_with_effects()` 尚未端到端实测过 FFmpeg filter_complex → 本轮必须真实跑通一次。

---

## 四、验收标准（本轮）

| # | 标准 | 判定方式 |
|---|------|---------|
| 1 | 火星层独立可见 | `embers_default.mp4` 单独播放，肉眼能看到小而少、快速运动、拖影成光条的火星（非黑屏） |
| 2 | 非黑自动校验 | 存在自动校验：对全黑/近黑叠加层会**生成失败报错**；用一个黑层做反向测试能触发失败 |
| 3 | 火星在最终视频跳出 | `particle_final.mp4` 中火星是离散明亮光点、明显亮于主体与背景，不像滤镜 |
| 4 | 颜色一致但不糊 | 火星与枪械同色系，但更亮更饱和、与主体有对比；整体不被染成一层平色 |
| 5 | 不喧宾夺主 | 火星是主要可见粒子效果；bokeh/暗角/边缘光从属，不盖过主体 |
| 6 | 03 旧标准不回退 | 运镜逐帧不变、帧间无闪烁、主体几何零变化（沿用 03 验收 #1/#2/#3，pixel-diff 校验） |
| 7 | 端到端真跑通 | `compose_mp4_with_effects()` 用真实 FFmpeg 跑通；非仅编译检查 |
| 8 | 本地双产物 | 同时产出**单独火星层**与**最终合成**两个视频文件路径，供启鸣人工验收（规则 R6） |

---

## 五、落地顺序

1. 先修**火星层可见性**（缺陷 1，致命，且是后续一切的前提）。
2. 加**非黑自动校验**，堵住静默失败。
3. **重平衡合成**：火星提亮、取色修正、bokeh/暗角降权（缺陷 2）。
4. 端到端真跑，产出单独层 + 最终合成两个视频。
5. 全程不引入 AI（沿用 ADR-006）。

---

## 六、待启鸣确认

1. 本轮聚焦「先把火星修到可见、再让它跳出来」，方向是否认可？
2. 取色规则修正——「同色系但必须更亮更饱和、不得糊成同色滤镜」——是否同意作为对 `ADR-006` 验收 #5 的细化？
3. 本 loop **不新开 ADR**（属执行纠偏，非新方法决策）；若你同意第 2 点，我会把该色彩细化作为一条**带日期的更新追加到 `ADR-006`**（沿用 ADR-004 的 dated-update 写法），而非新建 ADR。是否同意这样处理？
