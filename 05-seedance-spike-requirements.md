# Seedance 火花美化 Spike — 需求（探索性验证）

> 作者：Andy（对话 agent）· 状态：草稿，待启鸣 review
> 性质：**Spike（探索性、可丢弃）**，不触发 ADR。轻量验证，smoke 优先于完整工程化。
> 触发：loop 04 确定性火花虽可见但观感未达启鸣预期（整体观感：背景/质感/光影/氛围，**主要是火花**）。主体本身 OK，瓶颈在整体电影感。
> 路线选择：启鸣已定 **方案 2b**（Blender 出无火花结构底片 + 文字让 Seedance 生成火花美学）。方案 1（深度图直控）维持 ADR-005「已评估、暂缓」。

---

## 一、本 Spike 要回答的唯一问题

**Seedance 的 video-to-video（参考视频）能否在「保住运镜 + 不让主体漂移」的前提下，让火花与整体观感达到启鸣的预期？**

这是一个**只能实测、不能纸上判定**的问题：启鸣最在意的火花，恰是 AI 帧间一致最薄弱处（小+亮+高速运动）。Spike 的目的就是用最低成本跑出一段对照，让启鸣人眼判定「值不值得把 AI 火花纳入主线」。

---

## 二、方案 2b 的本质与边界（为什么这次可以试 AI 火花）

- **ADR-006 当初否决的是「AI 从零裸生成火花」** → 每帧重生成 → 火花位置/数量/亮度抖动 → 闪烁。
- **2b 不同：Blender 先出一版「无火花结构底片」**（主体 + 运镜 + 遮挡关系 + 光影，全部确定性烘死），把这段视频作为 Seedance 的**参考视频**，文字 prompt 只负责「在这个结构上长出好看的火花 + 氛围质感」。
- **风险被限制在火花本身**：主体几何、运镜轨迹、遮挡逻辑由确定性底片兜底，AI 即便发挥不稳，也不会扭曲枪械或歪掉运镜——崩只会崩在火花/氛围层。

**已查证的 Seedance 能力（2026-06）：** Seedance 2.0（4 月已上线 API）接受最多 3 段参考视频，主打对 performance/lighting/shadow/camera 的控制；有 video-to-video 美化的公开案例（绿幕替身实拍→AI 美化，运动结构保住）。**限制：** 它是「参考」而非硬约束，不接受 depth/相机矩阵作一等输入——保结构靠输入视频的结构惯性，非数学约束，故主体漂移风险存在但远小于纯文生/单图。2.5 更强但未公开、纯厂商宣称，本 Spike 以 **2.0 可用 API** 为准。

---

## 三、范围与分工（启鸣 2026-06-27 定）

**CC 不实际调用 Seedance API。** CC 的交付物只有两样：
1. **无火花结构底片视频的本地路径**（复用现有场景，渲一版主体 + 运镜 + 遮挡 + 光影、不含任何粒子的确定性底片）；
2. **设计好的提示词**（文字描述火花形态 + 背景氛围 + 质感 + 光影电影感）。

**启鸣本人**拿这两样去 Seedance 跑出成片 C，并自行人工验收。
→ 因此 **API 访问/key/渠道/额度不是本 Spike 的前提**，CC 无需查接入方式，无 blocker。

---

## 四、具体操作（轻量 Spike）

**CC 负责（仅两项交付物）：**
1. **出结构底片**：Blender 渲一段无火花底片（沿用已验收的运镜，主体+遮挡+光影，确定性，不含任何粒子）。输出本地视频路径。这是 Seedance 的结构真值输入。
2. **设计 prompt**：聚焦启鸣不满的四点——火花形态（拖影成光条、明亮飞溅）、背景氛围、质感、光影电影感。火花美学交给文字。可给 2-3 个 prompt 变体供启鸣挑。输出 prompt 文本。

**启鸣负责（CC 不做）：**
3. 拿底片 + prompt 去 Seedance 跑 video-to-video，生成成片 C；
4. 并排对照人工验收：基线 A（loop 04 确定性 `particle_final.mp4`）/ 底片 B（CC 出的无火花底片）/ 成片 C（Seedance 输出）。

---

## 五、验收标准（Spike 级，启鸣人工为主）

| # | 标准 | 判定方式 |
|---|------|---------|
| 1 | 整体观感提升 | 启鸣主观判定：C 是否在背景/质感/光影/氛围上明显优于基线 A（**这是 Spike 的核心目的**） |
| 2 | 火花符合预期 | 启鸣主观判定：火花形态/美感是否达到预期（出发点） |
| 3 | 主体不漂移 | 枪械几何/身份在 C 中保持，无明显形变/漂移（人眼 + 与底片对照） |
| 4 | 运镜保住 | C 的相机运动与底片一致，无歪斜/抖动 |
| 5 | 火花帧间一致 | **2b 最大风险点**：连续播放 C，火花无明显闪烁/抖动；若闪烁严重，即为 2b 的硬伤 |
| 6 | CC 交付物 | CC 输出：① 无火花底片视频本地路径；② prompt 文本（可多变体）。供启鸣拿去 Seedance（规则 R6） |

**结论判定：** 若 1/2 达标且 3/4/5 无硬伤 → 2b 成立，进入「是否纳入主线」的产品决策（届时触发 ADR 修订 ADR-006）。若 5（闪烁）成硬伤或 3（主体漂移）失守 → 2b 不成立，回退确定性火花，ADR-006 维持。

---

## 六、决策性质与后续

- 本 Spike **不触发 ADR**（探索、可丢弃，按 §8 评估：是探索性试验，非已定方法决策）。
- **若 Spike 通过** → 触发一份新 ADR：有界引入 AI 火花美化层（确定性底片打底 + AI 仅美化火花/氛围），作为对 ADR-006 的修订/补充。
- **若 Spike 失败** → 记录「已试、不成立」，回退确定性火花，ADR-006 维持不变。

---

## 七、确认状态（启鸣 2026-06-27 全部确认）

1. ✅ CC 只输出两样：要喂模型的**视频本地路径** + **提示词**；不调 API、不查接入方式（blocker 消除）。
2. ✅ Spike 范围同意。
3. ✅ 验收以启鸣人工主观判定为主。

→ 本需求已通过，进入 §8 ADR 评估（Spike 探索，不触发 ADR）→ 交接 CC 进 plan mode。
