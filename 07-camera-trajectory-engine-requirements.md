# 07 — 镜头轨迹引擎迁移（精控地基）— 需求（启鸣待确认）

> 作者：Andy（产品脑）· 审批/合并：启鸣（唯一审批人）· 语言：中文（人类决策文档）
> 上游：竞品分析 §7.2（镜头确定性 = 核心差异化）+ §四 tripwire T2（精确镜头输入 = 竞品至今未产品化的能力）。Loop 06 已把相机参数（height / start·end distance / focal_length / orbit·dolly）解耦进 JSON。
> 方向来源：启鸣 2026-06-29 选定「精细化镜头操作形成差异化壁垒」，迁移同项目废案（`R0xy-152/CineAnchor`，本地 `D:\CineAnchor`）的相机系统并优化。

---

## 一、背景

- 启鸣 定方向：精细化镜头操作是当前阶段要建的护城河。同项目废案有一套远比现管线成熟的相机系统：多关键帧 + Catmull-Rom 位置插值（C¹ 连续、速度平滑）+ SLERP 四元数插值（角速度恒定、始终单位四元数）+ look-at 四元数生成 + Three.js↔Blender 坐标转换 + 8 个电影级预设。
- 现管线只有 2 种运镜（dolly_in / orbit），每种仅 2 关键帧 + Bezier + ORTHO 相机——**表达不了一条任意手画的轨迹**，撑不起后续的交互式精控。
- 关键边界（已确认）：废案当年走的是**逐帧生成**方案，违背 ADR-004/005/006 的确定性原则，**不迁**。本期迁的是**纯数学插值引擎 + 预设数据**，产出关键帧喂给确定性 Blender 渲染。即「搬控制/数据层，不搬生成基质」。

## 二、目标

为后续「交互式精控」打地基：把废案的轨迹/插值引擎迁入本项目，使「任意多关键帧、平滑插值的相机轨迹」成为**可经 JSON 表达、可复现、可版本化**的数据，由确定性 Blender 渲染。

> 本 loop 是护城河的**地基（enabler）**，不是壁垒本体。交互控制层（键鼠 / 手柄 / 拖拽轨迹）留作下一 loop。

## 三、架构决策记录（本期定调，影响后续 loop）

经启鸣 2026-06-29 确认，镜头系统三层职责钉死：

- **渲染引擎层 = Blender（保留）**：被 JSON 驱动的后端，可用其相机/约束能力，但不作为用户操控面。
- **真相层 = 本项目 Project JSON**：相机轨迹必须是可复现、可 diff、可版本化的数据（= 竞品分析 top-3 的 Camera Reproducibility 落地前提）。
- **操控层（下一 loop）= 自研轻量 UI**：不嵌 Blender/UE5 原生编辑器窗口。废案为 Three.js，已有 `three_to_blender_pose()` 转换，可走「网页端轻量实时预览出 JSON 关键帧 → Blender 确定性出片」路线。

**明确否决**：① 不把 Blender/UE5 原生编辑器窗口当用户操控面（反向加重复杂度、状态存进可变场景文件 → 砸掉可复现护城河、沦为免费工具薄壳）；② 本期不引入 UE5（实时引擎 / 重运行时 / royalty / 不利无头批渲，属换赛道而非加功能，若将来需游戏级实时保真再单独评估）。

## 四、范围

### CC 做什么
1. **迁移插值引擎**（废案 `coordinate.py` → 纯 python，**禁止依赖 bpy**）：`catmull_rom`、`slerp`（含短路径翻转 + 近平行回退）、`interpolate_keyframes`、`three_to_blender_pose`、`target_from_quat`。
2. **迁移 8 个预设**（废案 `camera_presets.py`），适配 Blender Z-up。**预设本期作为引擎能力的验证/演示用例（demo），不承诺为正式产品功能/卖点。**
3. **接入渲染管线**：`CameraSpec` 增加可选 `preset` 字段；命中 preset 时走新增的关键帧相机分支，**未命中时现有 dolly_in / orbit 行为 100% 不变**（新增分支，非替换）。
4. **相机类型**：本期**允许引入 PERSP + FOV** 换取电影感（接受主体一定透视）；ORTHO 路径保留为默认/回退。
5. 单元测试（插值引擎 + 预设生成，纯 python，不依赖 Blender 环境）+ 默认路径零回归验证。

### CC 不做什么
- 不迁逐帧生成基质。
- 不做交互控制层（键鼠/手柄/拖轨迹）——下一 loop。
- 不嵌 Blender/UE5 原生编辑器窗口，不引入 UE5（见 §三）。
- 不改主体几何、文字流程。

### 启鸣 做什么
- review 本需求；后续 review CC 的执行计划（Andy 先把关 plan）。
- 抽验预设产出的运镜是否如描述、PERSP 下主体是否仅透视而无畸变崩坏。

## 五、验收标准

| # | 标准 | 判定方式 |
|---|------|---------|
| 1 | 插值引擎迁入且为纯 python（无 bpy 依赖），单测覆盖 catmull_rom / slerp（含短路径、近平行）/ interpolate / 坐标转换 / 边界（零时长、单帧、重复帧） | 单测通过 |
| 2 | 8 个预设均能产出合法关键帧（≥3 KF、字段齐全 t/pos/quat/fov、位置在 scene 合理范围）；`list_presets()` 返回 8 项 | 单测 + 抽验 |
| 3 | **默认路径零回归**（同 Loop 06 ADJ-3）：现有 dolly_in / orbit 输出像素级不变 | 重渲 pixel-diff = 0 |
| 4 | preset 经 JSON 命中即生效、生成对应运镜；不命中行为不变 | 渲染抽验 |
| 5 | **可复现性（核心）**：同一 preset/轨迹两次渲染 → 输出一致 | 两次渲染比对（对应竞品分析 top-3 Camera Reproducibility） |
| 6 | PERSP 路径产出可接受透视但**无主体畸变/几何崩坏**，与「素材保真」差异点不冲突 | 视觉抽验 |
| 7 | 输出全部新增/修改文件路径供 retrieval | CC 报告 |

## 六、ADR 评估（§8）

- 本 loop 是迁移 + 打地基，非新方向决策 → **不新开 ADR**。
- **引入 PERSP 相机**轻微触及 ADR-006 / 确定性「无畸变」边界：若 PERSP 成为常用路径，后续以带日期更新追加记录「允许 PERSP 运镜、ORTHO 仍为无畸变默认」。
- §三 架构决策（JSON = 真相 / Blender = 渲染后端 / 不嵌 DCC UI / 暂不引 UE5）先记入本需求；待**正式产品化交互控制**时，再评估是否升格为独立 ADR。

## 七、待确认问题

§四 的 3 个范围问题已确认（① 分期：本期只做地基 ② 预设当 demo ③ 接受 PERSP）。本需求据此编写，请 review 其余表述是否准确、有无遗漏。
