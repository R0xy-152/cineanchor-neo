# 08 — 交互式相机控制（Interactive Camera Control）— 需求

> 作者：Andy · 审批：启鸣 · 语言：中文
> 状态：**草稿，待 启鸣 review**
> 上游：`07-camera-trajectory-engine-{requirements,plan,acceptance}.md`（引擎+预设已签收）；`ADR-005`（路线排序/四条硬标准）；`00-competitive-analysis.md` §四 护城河 T2「精确相机输入」/T3「可复现」。

---

## 一、本 loop 定位（为什么做这个）

Loop 07 交付的是**地基**：纯数学插值引擎 + 8 预设（demo）。但预设只是「选菜单」，**不是护城河**。
真正的差异化（竞品分析 T2）是：**让用户用键鼠/手柄像玩游戏一样，所见即所得地摆出运镜，并精确落成 JSON 关键帧**——这是 Seedance 这类「抽奖机」给不了的「生产线级精确输入」。

本 loop 把 07 的离线引擎，接上一个**浏览器内实时预览 + 交互输入**的前端，输出仍是既有的 `CameraSpec` 关键帧 JSON。**渲染后端不变**（Blender Eevee 确定性出片），交互只负责「生成关键帧」，不碰渲染管线。

一句话：**loop 07 = 能算轨迹；loop 08 = 人能用手摆轨迹，且摆出来的和渲出来的一致。**

## 二、范围（明确收口）

**做（本 loop 内）：**
1. 浏览器内 Three.js 实时预览视口：加载与渲染同一份资产（GLB/PNG）+ 同一相机参数，实时显示当前机位看到的画面。
2. **键盘 + 鼠标飞行式机位控制**（P0，人人都有）：WASD/QE 平移、鼠标拖拽转向、滚轮推拉（dolly），FPS/DCC 通用手感。
3. **关键帧捕获 + 时间轴**（P0）：当前机位「打点」存为关键帧；时间轴上可看/选/删关键帧；预览可沿时间轴 scrub 播放（复用 07 的 Catmull-Rom + SLERP 插值，**逻辑与 Blender 端同源**）。
4. **预览/出片一致性校准（parity，本 loop 的 make-or-break）**：浏览器预览的取景，必须与 Blender 实渲在容差内一致（坐标系、镜头/FOV、画幅比）。
5. **carry-forward 闭合**：PERSP 下 文字叠加定位（FOV→view_height）实测验证（loop 07 遗留）。

**不做（显式推迟）：**
- **手柄控制** → 推迟到 loop 09（P1；键鼠手感跑通+parity 锁定后再加输入设备，避免一上来铺三种输入稀释 parity 这个真问题）。
- **3D 轨迹曲线直接拖拽编辑**（在视口里拖控制点改样条）→ 推迟到 loop 09（P1）。本 loop 关键帧编辑限定在「打点/删点 + 时间轴」。
- 嵌入 Blender/UE5 编辑器到前端（loop 07 §三已否决）。
- 任何把渲染交给生成式 AI 的路径（违背四条硬标准/ADR-005）。

## 三、架构衔接（沿用 loop 07 决策，不重开）

```
浏览器 Three.js 视口(交互预览)  ──打点/编辑──▶  CameraSpec 关键帧 JSON  ──▶  FastAPI ──▶ Blender Eevee 渲染 ──▶ FFmpeg ──▶ MP4
        ▲ 同源插值(camera_math 逻辑)                    （真相，唯一来源；可复现/可 diff/可版本化）
```

- **JSON 仍是唯一真相**：交互的每一步都序列化进既有 `CameraSpec`（07 的 `preset` 字段 + keyframes），不新增并行真相源。
- **插值同源**：预览用的插值必须与 `camera_math.py` 行为一致（移植到 JS 或共享同一份参数定义），否则 parity 无从谈起。
- **渲染零改动**：本 loop 不动 `render_project.py` 的渲染逻辑，只消费它产出的 JSON。默认路径继续保持 loop 07 的逐位一致保证。

## 四、非协商项（硬门槛，任一不过则不签收）

1. **预览/出片一致性（parity）**：同一份 JSON，浏览器预览取景 vs Blender 实渲，关键地标（主体边界、画面中心）误差在约定容差内（建议 ≤ 画幅 2% 或人眼不可分辨）。这是本 loop 存在的理由，必须有量化闸门。
2. **JSON 往返无损**：JSON → 预览 → 交互编辑 → 导出 JSON，未改动的字段逐字段一致；交互产生的关键帧能被 Blender 端原样渲染。
3. **默认渲染路径零回归**：不带交互关键帧的既有项目，渲染输出与 loop 07 基线逐位一致（沿用 ADJ-2 口径）。
4. **确定性边界不破**：交互只生成关键帧，渲染仍是确定性 Blender；无逐帧、无 AI 介入控制路径。
5. **carry-forward 闭合**：PERSP 预设带 `title` 实渲一发，确认文字在 PERSP 投影下定位正确（不偏、不溢出）。

## 五、验收表（待 review，CC plan 阶段细化判定方式）

| # | 验收标准 | 判定方式（建议） | 阻塞? |
|---|---------|----------------|------|
| 1 | 浏览器视口加载资产并实时显示当前机位画面 | 人工 + 截图 | 是 |
| 2 | 键鼠飞行控制可平移/转向/推拉，手感可用 | 启鸣 人工试用 | 是 |
| 3 | 关键帧打点/删点/时间轴 scrub 正常，插值与 07 同源 | 单测(JS 插值 vs camera_math 对拍) + 人工 | 是 |
| 4 | **parity**：预览取景 vs Blender 实渲在容差内一致 | 视觉闸门(同 JSON 双端取景对比，量化误差) | **是(核心)** |
| 5 | JSON 往返无损 + 交互关键帧可被 Blender 原样渲染 | 单测 + 渲染抽验 | 是 |
| 6 | 默认路径零回归（无交互关键帧 → 逐位一致） | pixel-diff（沿用 ADJ-2） | 是 |
| 7 | carry-forward：PERSP + `title` 文字定位正确 | 实渲 1 发 + 人工 | 是 |
| 8 | 本地视频/截图产物 + 全部新增/改动文件路径 | CC 报告 | 是 |

## 六、留给 启鸣 review 的开放点（请拍板）

1. **MVP 收口认可否**：本 loop 只做「键鼠 + 打点时间轴 + parity」，手柄 & 轨迹拖拽编辑推迟 loop 09。同意 / 调整？
2. **parity 容差口径**：用「画幅百分比误差」还是「关键地标像素误差」还是「人眼不可分辨即可（practical pass，同 ADJ-3）」？倾向后者 + 一个量化兜底。
3. **预览插值实现**：camera_math 移植到 JS（前端纯算、零后端往返、手感最好）vs 前端每帧调 API 用 python 算（绝对同源、但有延迟）。倾向移植到 JS + 用单测对拍保证同源。
4. ORTHO/PERSP 在预览里都要支持，还是预览先只锁 PERSP（交互手感场景基本都是 PERSP）？
