# 10 — 角色生日模板 MVP — 需求（Requirements）

> 作者：Andy（需求收敛）· 待启鸣 pass/reject
> 归属：V0.2 里程碑 · P0 第一交付物
> 前置：`V0.2-vision-and-architecture.md`（大方向）已定；`docs/reference_analysis/reference_video_analysis.md` 应先行

---

## 一、定位

V0.2 的**第一个、也是核心验收物**：用户上传角色立绘 + Logo、填角色名/生日/标题/字幕、选主题色，系统稳定产出一条竖屏（9:16, 1080×1920）角色生日/生贺视频。

**本 loop 只做 15 秒 MVP 版**（控制开发量、先跑通端到端），完整 27 秒版推后。

## 二、范围

### 2.1 用户输入

| 输入项 | 必填 | 说明 |
|--------|------|------|
| 主角色 PNG | 是 | **必须已抠好透明背景**（系统不 auto-matting，见大方向 §4.3） |
| 游戏 Logo | 是 | 片尾用 |
| 角色名 | 是 | 中英文均可 |
| 生日日期 | 是 | 例 `07.01` |
| 主标题 | 是 | 例 `HAPPY BIRTHDAY` |
| 2–3 句台词字幕 | 否 | 底部白字黑描边 |
| 主题色 | 否 | 缺省自动给一套 |
| 补充图片 | 否 | 小头像 / 卡片装饰 |
| BGM | 否 | **V0.2 先不做** |

**缺任一可选项不得崩溃**，仍能导出。

### 2.2 视频结构（15s MVP 版，锁定）

| 时间段 | 画面 |
|--------|------|
| 0–2s | 大号生日日期卡 |
| 2–4s | 角色名 + 主标题（标题条） |
| 4–9s | 角色海报页（立绘 + 缓推镜头） |
| 9–13s | 台词字幕 + 花瓣粒子 |
| 13–15s | Logo 片尾 |

### 2.3 镜头运动（引擎内化，用户不碰）

- 角色海报页的「缓推 / 轻移」由**内部镜头引擎**（loop 07-09 preset/keyframe）生成，用户看不到镜头参数。
- 立绘轻动效替代实机片段（无实机时的兜底）。

### 2.4 粒子

- 花瓣/光效 = **ADR-006 确定性方法**，无 AI（大方向 §4.2）。

## 三、架构约束

- Project JSON 新增 `character_birthday` 模板类型，**不破坏** `character_intro` / `product_orbit`。
- 模板 = JSON 生成器；渲染后端不为模板改动；JSON 仍唯一真相。
- 预览沿用 Three.js（loop 09 spike 定）。
- **本 loop 不抽象组件库**（大方向 §五）——生日模板可先具体实现，组件抽象等枪皮模板后。

## 四、不可谈判项

1. 主角色 PNG 必须透明背景，系统不抠图（缺则明确报错，不硬渲）。
2. 无 AI：粒子确定性；渲染链纯确定性。
3. 旧模板（character_intro / product_orbit）零回归。
4. 缺可选素材不崩、仍可导出。
5. **文字定位过视觉闸门**（日期卡/标题条/字幕都要人眼确认不偏、不溢出）。

## 五、验收表

| # | 标准 | 判定方式 | 阻塞 |
|---|------|---------|------|
| 1 | 传角色 PNG + Logo + 填角色名/日期/标题 → 导出 9:16 1080×1920 MP4 | 端到端人工跑通 | 是 |
| 2 | 视频含 5 段结构（日期卡/标题条/海报缓推/字幕+花瓣/Logo 片尾） | 启鸣 目视 | 是 |
| 3 | **文字定位正确**（日期卡/标题条/字幕不偏不溢出，可读） | **视觉闸门 + 启鸣目视** | **是** |
| 4 | 花瓣粒子确定性（两次渲染一致） | pixel-diff / 单测 | 是 |
| 5 | 缺可选素材（无字幕/无补充图/无主题色/无 BGM）仍导出不崩 | 单测 + 抽验 | 是 |
| 6 | 旧模板零回归 | 全量测试 + pixel-diff | 是 |
| 7 | JSON schema 校验通过，新模板可被 Blender 原样渲染 | 单测 + 渲染抽验 | 是 |
| 8 | 一条可展示样例视频 + 使用说明文档 | CC 产物 | 是 |

## 六、待启鸣确认 / 我锁定的决策

以下按大方向文档已定，若无异议即锁进本 loop：
1. **只做 15s MVP 版**（27s 版推后）。
2. 主角色 PNG 透明背景硬前提。
3. 缺省主题色自动给一套（具体配色 CC plan 阶段给候选，Andy 定）。
4. 立绘轻动效作为无实机片段时的兜底。

## 七、备注

- **reference_video_analysis 先行**：模板结构应从真实生日/生贺竞品视频反推校准，不拍脑袋。若分析显示结构需调，回来改本需求。
- ADR：本需求验收后，Andy 起草模板化架构 ADR（含 JSON schema 扩展 + 镜头引擎内化 + 确定性边界）。
