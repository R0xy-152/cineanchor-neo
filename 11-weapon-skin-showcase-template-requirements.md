# 11 — 枪械皮肤展示模板 — 需求（Requirements）

> 作者：Andy（需求收敛）· 待启鸣 pass/reject
> 归属：V0.2 里程碑 · P1（生日模板之后）
> 前置：生日模板（loop 10）先跑通；`V0.2-vision-and-architecture.md` 已定

---

## 一、定位

V0.2 第二个模板：用户上传枪械图 + Logo、填武器名/皮肤名/稀有度/主题/主题色，产出一条竖屏（9:16）枪械皮肤展示视频。重点**不是复杂镜头**，而是：**枪械主体 + 暗场背景 + 主题色灯光 + 光扫 + 元素粒子 + 皮肤标题**。

**本 loop 允许「设计文档先行 + 最小 PNG 版实现」**（大方向 §七）：PNG 做 2.5D 展示；GLB 3D 展示明确后置。

**关键价值：验证组件抽象。** 这是 V0.2 的第二个模板——做它时，与生日模板真正共享的形状会浮现，**组件库在此 loop 后才抽**（大方向 §五，反投机抽象）。

## 二、范围

### 2.1 用户输入

| 输入项 | 必填 | 说明 |
|--------|------|------|
| 枪械 PNG | 是 | **透明背景**；本 loop 做 PNG 2.5D（GLB 3D 后置） |
| 游戏 Logo | 是 | 片尾 |
| 武器名 | 是 | 例 `AK-47` |
| 皮肤名 | 是 | 例 `Neon Requiem` |
| 稀有度 | 否 | Legendary / Epic 等 |
| 主题色 | 是 | 决定灯光与粒子基调 |
| 元素主题 | 是 | 火焰 / 冰霜 / 雷电 / 毒液 / 赛博 / 金黑 等 |
| CTA | 否 | 限时上架 / 即刻获取 |

缺可选项不崩、仍可导出。

### 2.2 视频结构（12s 版，锁定）

| 时间段 | 画面 |
|--------|------|
| 0–1s | 暗场中主题色光亮起 |
| 1–3s | 枪械轮廓逐步显现 |
| 3–5s | 光扫枪身，展示纹理 |
| 5–7s | 枪械完整侧面展示 |
| 7–9s | 元素特效爆发（对应元素主题） |
| 9–10.5s | 皮肤名 / 稀有度 / 系列名 |
| 10.5–12s | Logo + CTA 片尾 |

### 2.3 镜头 / 灯光 / 粒子（引擎内化，确定性）

- 「枪身侧面展示 / 轮廓显现」运动 = **内部镜头引擎**生成，用户不碰参数。
- 主题色灯光脉冲、光扫 = 确定性渲染（按主题色驱动）。
- 元素粒子（火/冰/电…）= **ADR-006 确定性方法**，无 AI。

## 三、架构约束

- Project JSON 新增 `weapon_skin_showcase` 模板类型，**不破坏**已有模板。
- PNG 2.5D 路径优先；GLB 3D 路径明确后置（本 loop 可只留 schema 占位 + 设计说明）。
- 模板 = JSON 生成器；渲染后端不为模板改；预览沿用 Three.js。

## 四、不可谈判项

1. 枪械 PNG 透明背景，系统不抠图。
2. 无 AI：灯光/光扫/粒子确定性。
3. 旧模板 + 生日模板零回归。
4. 缺可选素材不崩。
5. **皮肤名/稀有度/CTA 文字定位过视觉闸门。**
6. **组件抽象在本 loop 收尾时评估**：与生日模板的真实共享形状 → 才抽（不预先抽）。

## 五、验收表

| # | 标准 | 判定方式 | 阻塞 |
|---|------|---------|------|
| 1 | 传枪械 PNG + Logo + 填武器名/皮肤名/主题色/元素 → 导出 9:16 12s MP4 | 端到端人工 | 是 |
| 2 | 视频含 7 段结构（暗场亮起→轮廓→光扫→侧展→元素爆发→皮肤名→Logo+CTA） | 启鸣 目视 | 是 |
| 3 | 主题色正确驱动灯光/粒子基调；元素主题对应特效 | 启鸣 目视 | 是 |
| 4 | **文字定位正确**（皮肤名/稀有度/CTA 不偏不溢出，可读） | **视觉闸门 + 启鸣** | **是** |
| 5 | 灯光/光扫/粒子确定性（两次渲染一致） | pixel-diff | 是 |
| 6 | 缺可选素材仍导出不崩 | 单测 + 抽验 | 是 |
| 7 | 旧模板 + 生日模板零回归 | 全量测试 | 是 |
| 8 | **组件抽象评估报告**：与生日模板共享形状清单 + 抽/不抽建议 | Andy review | 是 |
| 9 | 样例视频 + 使用说明 | CC 产物 | 是 |

## 六、待启鸣确认 / 我锁定的决策

1. **本 loop 允许设计文档先行 + PNG 最小版**；GLB 3D 后置。
2. 枪械 PNG 透明背景硬前提。
3. 元素主题作为预设集（火/冰/电/毒/赛博/金黑），具体特效 CC plan 给方案、Andy 定。
4. 组件库在本 loop 收尾评估后才抽（§8 验收）。

## 七、备注

- reference_video_analysis 应覆盖枪皮竞品，校准 7 段结构与元素特效。
- ADR：与生日模板共用「模板化架构 ADR」；若组件抽象形成稳定模式，Andy 追加 ADR 记录组件契约。
