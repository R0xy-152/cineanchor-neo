# 12 — 确定性粒子与动效系统 —— Requirements（启鸣提出，Andy 收敛）

> 语言：需求 = 中文（人的决策）。归属：repo root loop 文档。
> 缘起：loop 10（生日）粒子/动效达不到生产标准；loop 11（武器皮肤）返工后火星特效人工视觉**仍看不见**。两个模板同源信号 → 把"粒子/动效"从各模板的附属处理，抽成一个**独立、跨模板、确定性**的子系统单独一个 loop 做。抽象时机正当（第 2 个模板已确认共性，非过早抽象）。

---

## 一、目标

交付一个**跨模板、确定性、可复现**的粒子 + 动效模块，两个层面都要达标：

1. **正确性**：该出现的粒子必须真的发射、在正确的时间段可见；该被 `theme_color` / `element_theme` 驱动的必须真的被驱动。（直接修 loop 11 "火星根本不出现" 的缺陷。）
2. **质量**：达到"宣传片生产标准"，不能"廉价 / 假"。（修 loop 10 "漂浮粒子 + 动效需改善"、loop 11 第一版火星"太廉价太假"。）

**本 loop 是"确定性能否达到生产线"的决定性验证。** 结果直接决定是否需要激活 ADR-010（有界离线 AI 层）——见 §五退出条件。

## 二、范围

- 覆盖两个已有模板的粒子/动效：**生日**的漂浮粒子；**武器皮肤**的 `element_burst` 火/元素特效。
- 覆盖 `element_theme` 全部 6 种预设（fire / ice / lightning / venom / cyber / gold_black）。
- `theme_color` 联动：粒子基色 + 主光随主题色变化。
- 全程**确定性**：seed 来源明确、同输入两次渲染 pixel 一致。
- **不含任何 AI**（逐帧或生成式），沿用 ADR-006 确定性方法。

## 三、具体要求（可验收）

1. **触发正确性**
   - 每个模板、每个 `element_theme` 的粒子在其应出现的时间段**实际发射且肉眼可见**。
   - 武器皮肤 `element_burst`（约 7–9s）必须出现对应元素特效；6 种元素逐一验证。

2. **方法质量**（都在确定性范围内，逐条对应 loop 11 RW-4）
   - sprite 贴图，**非纯色点**。
   - **additive / screen 叠加**（廉价感主要来自普通 alpha 叠加）。
   - **运动**：速度 + 生命周期衰减 + 上升热气流 + 拖尾，非原地闪烁。
   - **联动上色**：跟 `theme_color` / `element_theme`（fire 暖橙、ice 冷蓝……）。
   - 配合 `light_sweep` 做局部辉光 / 热浪，增强氛围。

3. **动效阶段感**：关键帧动效读起来有"起—承—转—合"的宣传片节奏，不是静态闪烁堆叠。

4. **确定性**：同一 Project JSON 两次渲染 pixel-diff 一致；seed 来源在文档中写明（2D 路径 oracle 不覆盖，需自带 pixel-diff 校验）。

5. **人工视觉门（硬门）**：启鸣在样片里能**清楚看见**火星/粒子，并判定"不廉价、达生产标准"。对照 loop 11 血泪：**绿灯 ≠ 可见，可见 ≠ 不廉价**——单元测试全绿不构成通过。

6. **跨模板复用**：生日 + 武器皮肤都接入**同一个** FX 模块；接入后两模板回归不破坏（含既有确定性回归）。

## 四、验收标准

1. 生日样片：漂浮粒子 + 动效达生产标准（启鸣目视判定）。
2. 武器皮肤样片：火星在 `element_burst` 段**清楚可见且不廉价**（对照两版失败：第一版"廉价假"、返工版"看不见"）。
3. 6 种 `element_theme` 各出一帧证明其粒子发射且上色正确。
4. 两次渲染 pixel-diff 一致（确定性）。
5. 两模板回归全绿（生日 + 武器皮肤 + legacy 三模板不退化）。
6. 与 loop 10/11 收尾对接：loop 10/11 用本模块**重渲**后，其粒子相关收尾项一并签收（本 loop 是 loop 10 收尾项 (b) 的实际交付载体）。

## 五、决策触发点（本 loop 的显式退出条件）

> 尽最大确定性努力（§三全部做到）后，**若火星/粒子仍达不到生产 bar** → 这**不是失败，是决策信息**：触发 **ADR-010（有界离线 AI 层）** 的 Andy/启鸣 联合决策。
> 有界的定义（草案见 ADR-010）：AI 只在**渲染前一次性**生成**静态、可版本化、可人工审阅**的素材（如火焰贴图 / 短循环序列帧），渲染管线本身仍确定性逐帧合成——绝不逐帧重绘、绝不进实时预览。这样帧间一致靠"生成一次静态素材"来保。
> **CC 不得单方面引入 AI**；达不到 bar 时如实上报，由启鸣拍板是否激活 ADR-010。

## 六、非目标（本 loop 不碰）

- 不引入逐帧 / 实时 AI。
- 不做 AI 素材生成 / AI 台词 / AI 配音——那些属 V0.3 方向，记在 `V0.3-roadmap-notes.md` + ADR-010 草案，**本 loop 不实现**。
- 不改模板的取景 / 相机 / 文字排版（那些是 loop 10/11 各自的收尾项，与本 loop 解耦）。
