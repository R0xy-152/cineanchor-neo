# Loop 13：AI 静态素材质感判定实验（Requirements）

> 作者：Andy（产品收敛）· 待启鸣验收
> 状态：草案 · 日期：2026-07-03
> 前置：loop 12 确定性 FX 系统已交付（引擎正确性达标，启鸣人眼判"美术质感不可用于生产"）
> 性质：**判定实验，非架构决策**。目的 = 用最小代价回答"要不要转逐帧生成 AI"，而不是直接转。

---

## 一、为什么做这个实验（背景）

loop 12 把确定性 FX 引擎的**工程正确性**做到了（3D 枪口锚定、闭式确定性、量化门、深度遮挡全过）。但启鸣人眼判决：**美术质感仍不可用于实际生产**。

启鸣的核心怀疑（2026-07-03）：
1. 确定性引擎本身不会"判断"美术质感——它只忠实执行喂给它的 sprite；质感瓶颈在**素材**，不在引擎。（Andy 同意）
2. 随手做的好素材未必**适配主体**（通用火花配暗黑枪皮/傲娇少女大概率不搭）。（Andy 同意）
3. 由此启鸣推断：只有**经视频训练、"理解"美术质感的 AI** 才能解决。

**本实验要检验的命题**：启鸣怀疑（1）（2）为真 → 但能否推出"必须逐帧生成整片"？
本实验用**静态产物路**（ADR-010）先满足怀疑（1）（2）：让懂质感的 AI **离线生成适配主体的高质量素材**，喂进现有确定性引擎重渲，看质感与适配性是否达标。

- **若达标** → 启鸣诉求已满足，且不丧失主体保真/可控/可复现，不必重开 ADR-004。实验成功、判定终结。
- **若仍不达标，且能定位到"问题在必须由 AI 接管逐帧运动，而非素材质感"** → 这才构成逐帧生成的**充分证据**，触发 ADR-004 修订 + ADR-010 升级的联合决策。

> 本实验遵循 ADR-010 的关键区分：**预渲染静态产物 AI = 允许**（离线/静态/种子化/版本化/人审）；**逐帧像素 AI = 本实验明确不做**。素材由 AI 离线生成一次，逐帧合成仍由现有确定性引擎完成——主体保真、可控、可复现一个都不丢。

## 二、范围

**做：**
- **模型选择权归启鸣**：CC/codex **不自行选定 AI 模型**。先输出**参考视频 / 参考图 + 建议 prompt**（说明每个候选模型的质感倾向、成本、离线可行性）交启鸣，**由启鸣挑选模型**后再正式生成素材。
- 用启鸣选定的 AI 图像模型**离线生成静态火花 / 火焰 / 粒子 sprite 素材表**（PNG，带 alpha），作为现有 `config/fx_presets.json` sprite 的替换来源。
- **按主体/主题生成适配素材**：至少覆盖 2 个反差明显的主体语境，验证"适配主体"这个怀疑——
  - 语境 A：暗黑/金属风枪皮（fire 或 lightning 元素）。
  - 语境 B：柔和/可爱风生贺（花瓣/光点）。
- 生成的素材接入**现有 loop 12 确定性引擎，逐帧合成路径完全不变**，重渲对应片段。
- 素材必须**种子化 + 版本化 + 人审留痕**（ADR-010 准入条件）：记录模型、prompt、seed、素材 SHA-256、生成时间、审阅人。

**不做（硬边界）：**
- **不做逐帧视频生成 / video2video / 任何对成片帧的 AI 后处理**。AI 只产出**离线静态 PNG 素材**，产出后 AI 即退出管线。
- 不重画/不覆盖客户英雄主体（枪身、角色、logo）像素。
- 不改确定性引擎的合成逻辑、不改 3D 枪口锚定、不改量化门。
- 不改 ADR（ADR 修订是 Andy 的活）；本轮结论写进执行记录，供后续 ADR 决策引用。
- 不因本实验放松 loop 10 真实主体文字门 / loop 11 整片取景（各自独立欠账）。

## 三、要求

1. **素材生成可复现**：同 prompt + 同 seed → 同素材（或素材固化后版本锁，二选一，须说明）。生成过程离线一次性，不进正式渲染的每次调用。
2. **素材质量**：sprite 为真实美术质感火花/火焰纹理（非程序圆点），带 alpha、可加叠加/屏幕混合、可染主题色。
3. **主体适配验证**：语境 A / B 各出一套素材，肉眼可辨风格差异（暗黑金属 vs 柔和可爱），且各自与主体搭。
4. **接入不破坏确定性**：素材替换后，loop 12 的闭式确定性、3D 锚定、深度遮挡、量化门**全部仍通过**（AI 只换素材，不换引擎）。
5. **对照组**：同一片段出两版——(a) loop 12 现有程序 sprite 版，(b) AI 素材版——供启鸣**并排人眼对比**判质感提升幅度。
6. **视频验收闭环（R6，强制）**：CC/codex 本地渲染，输出**两版 1080×1920 MP4 文件路径**供启鸣人眼判决。Andy 沙箱无法渲染。

## 四、验收（启鸣人眼判决，非量化门）

**前置交接（模型选择）**：CC/codex 先交**参考视频/参考图 + 建议 prompt + 候选模型说明** → **启鸣选定模型** → CC/codex 用选定模型生成素材、接入引擎、渲染两版样片。

启鸣并排对比两版样片，判三点：
1. **质感**：AI 素材版是否达到"可用于实际生产"的美术质感（这是 loop 12 卡住的那关）。
2. **适配性**：AI 素材是否与主体风格相搭（验证怀疑 2）。
3. **判定路由**：
   - **过** → 静态产物路成立，启鸣诉求满足，不必逐帧生成。实验成功。Andy 起草 ADR-010 状态更新（静态素材生成 = 已验证的准入激活）。
   - **不过，且启鸣判定"素材质感已够、问题在缺少 AI 逐帧运动/理解"** → 构成逐帧生成充分证据，Andy 起草 ADR-004 修订 + ADR-010 升级的联合决策草案，交启鸣拍板。
   - **不过，但问题仍在素材本身** → 继续静态路调优，不升级到逐帧。

## 五、与 1 LSB 问题的关系（澄清，避免混淆）

loop 12 遗留的"独立全片 RGB bit-exact 未达标（≤1 LSB 浮点底噪）"**与本实验无关**，**不走 ADR-010**。那是确定性容差定义问题（ADR-003/005 范畴），由 Andy 单独起草 ADR-005 容差复现补丁处理（run-to-run ≤1 LSB 感知底噪 = 可接受，bit-exact 非基准）。请勿把它和本实验的 AI 判定混为一谈。

## 六、非目标

逐帧 AI 视频生成 / video2video / 成片 AI 后处理 / 自由粒子编辑器 / 新模板 / 改确定性引擎合成逻辑 / 放松既有视觉门。（这些均不在本判定实验内；是否逐帧生成，取决于本实验 §四 的判定路由结果。）
