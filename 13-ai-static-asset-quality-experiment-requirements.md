# Loop 13：video2video 质感增强判定实验（Requirements）

> 作者：Andy（产品收敛）· 待启鸣验收
> 状态：草案 · 日期：2026-07-03
> 前置：loop 12 确定性 FX 系统已交付（引擎正确性达标，启鸣人眼判"美术质感不可用于生产"）
> 性质：**受控判定实验**，用于**重估 ADR-004 的前提**。不是"顺手试 AI"，而是带客观失败判据去证伪/证实一条不可逆架构决策。

---

## 一、为什么做这个实验（背景与决策）

loop 12 把确定性 FX 引擎的**工程正确性**做到了（3D 枪口锚定、闭式确定性、量化门、深度遮挡全过）。但启鸣人眼判决：**美术质感仍不可用于实际生产**。

启鸣的判断（2026-07-03）：
1. 确定性引擎不会"判断"美术质感——瓶颈在**素材/表现**，不在引擎。（Andy 同意）
2. 只有经视频训练、"理解"美术质感的 AI 才可能真正解决质感问题。
3. **产品护城河 = 运镜确定性**（ADR-001/008）。因此设想：让 AI 只做**质感增强**，运镜仍由确定性引擎掌控 → 或可两全。
4. 关于闪烁/漂移：启鸣认为商业 AI 视频引擎**不会**出现（早期问题源于本地 Comfy+diffusion 不稳定，非前沿商业模型）。**Andy 已让步此点**——闪烁不再作为反对理由。
5. 三个具体威胁（下 §三）**启鸣认可真实存在，以实验结果为准**。

**决策（启鸣拍板）：先做 video2video 尝试；失败后再退回"AI 离线静态素材 → 确定性引擎合成"这条路。**

- video2video = 把确定性引擎渲染好的片段喂给商业 AI 视频模型做**质感增强**，运镜/主体理应仍来自我们的确定性渲染。
- 本实验存在的意义：video2video 属**逐帧像素 AI**，是 **ADR-004 当前明确禁止、ADR-010 划为 FORBIDDEN** 的一类。要做，就作为一次**受控实验去重估 ADR-004 的前提**，带客观判据、拿证据回来，而非无证据地赌掉护城河。

## 二、范围

**主线（先做）：video2video 质感增强尝试**
- **模型选择权归启鸣**：CC/codex **不自行选定模型**。先输出**参考视频/参考图 + 建议 prompt + 候选模型说明**（质感倾向、成本、是否可复现/是否远程）交启鸣，**由启鸣选定模型**后再正式跑。
- 输入 = 确定性引擎已渲染好的枪皮片段（含既定运镜）；AI 只做**质感/表现增强**，理应不改运镜、不改主体。
- 至少覆盖 2 个反差主体语境，验证"适配主体"：
  - 语境 A：暗黑/金属风枪皮（fire 或 lightning 元素）。
  - 语境 B：柔和/可爱风生贺（花瓣/光点）。
- **对照组**：同一片段出两版——(a) loop 12 确定性版，(b) video2video 增强版——供启鸣并排人眼对比。
- 全程按 §三失败判据 A/B/C 采证。

**回退线（主线失败后才做）：AI 离线静态素材 → 确定性引擎合成**
- 命中任一失败判据 → 停止 video2video，转此路：AI **离线**生成适配主体的高质量静态 sprite 素材（PNG+alpha），喂进现有确定性引擎逐帧合成，运镜/主体完全由引擎掌控。
- 此路符合 ADR-010 静态产物准入，不重开 ADR-004。

## 三、三个具体威胁 = 三条客观失败判据（命中任一即判 video2video 此路不通，转回退线）

护城河有两半：**运镜确定性 + 主体保真**。加可复现，共三条。任一被 video2video 破坏，即达失败：

- **判据 A — 主体保真**：AI 输出的枪身刻纹/角色脸/logo 与原确定性渲染帧比对，主体像素被改写超出约定容差 → **失败**（护城河的主体那半丢了；运营物料里重画过的官方主体 = 废稿）。
- **判据 B — 运镜保真**：输出片段的相机运动与确定性引擎 golden-pose 轨迹偏离超容差（oracle 复核）→ **失败**（护城河的运镜那半被 AI 侵蚀 = 自毁卖点）。
- **判据 C — 可复现**：同输入两次调用输出差异不可接受，或强依赖会更新/下线的远程服务，做不到 same-input→same-video → **记为可复现性缺陷**（ADR-001 运营版本化重渲诉求结构性无法满足），连同 A/B 交启鸣做最终架构判决。

> 容差数值由 CC/codex 在 plan 阶段提出、Andy plan-review 时定；A/B 以 loop 08 既有标准为基线（landmark ≤2% / oracle 干净剪影），不新造宽松标准。

## 四、要求

1. **对照可判**：确定性版 vs video2video 版必须同片段、同运镜、可并排。
2. **采证优先**：本实验第一产出是**判据 A/B/C 的证据**，其次才是"好不好看"。即使质感惊艳，若命中 A/B/C 仍判 video2video 不可用于本产品。
3. **回退线素材版本锁（仅回退线适用）**：若进入回退线，凡进入正式渲染的 AI 素材文件必须**版本锁 + SHA 存档**——理由是 ADR-003「同 JSON+同 seed → 同视频」：素材被换会导致同一 JSON 渲出不同视频，破坏可复现。**注意：要锁的是成品素材文件，不是生成它的 AI 过程**（生成过程无需可复现，一次性离线产出即可）。
4. **视频验收闭环（R6，强制）**：CC/codex 本地渲染，输出**两版 1080×1920 MP4 文件路径**供启鸣人眼判决。Andy 沙箱无法渲染。
5. **不放松既有欠账**：本实验不覆盖 loop 10 真实主体文字门、loop 11 整片取景，各自独立。

## 五、验收与判定路由（启鸣人眼判决 + 客观判据）

**前置交接（模型选择）**：CC/codex 先交参考视频/图 + 建议 prompt + 候选模型说明 → **启鸣选定模型** → 再正式跑 video2video、渲两版样片、采 A/B/C 证据。

判定路由：
- **A/B/C 全过 且 启鸣人眼判质感达生产标准** → video2video 成立:护城河两半（运镜+主体）都没丢、可复现，且质感解决。**ADR-004 前提确应修订** → Andy 起草 ADR-004 修订 + ADR-010 升级（受控 video2video 准入）交启鸣拍板。
- **命中判据 A 或 B 或 C（任一）** → video2video 破坏护城河/可复现，**判此路不通** → 按启鸣决策**转回退线**（AI 离线静态素材 → 确定性引擎合成），此路无需重开 ADR-004。实验"失败"至此有客观触发点，非凭感觉。
- **A/B/C 全过 但 启鸣人眼判质感仍不达标** → AI 逐帧也救不了质感 → 回退线 + 继续确定性/素材调优。

## 六、与 1 LSB 问题的关系（澄清，避免混淆）

loop 12 遗留的"独立全片 RGB bit-exact 未达标（≤1 LSB 浮点底噪）"**与本实验无关**，**不走 ADR-004/010 决策**。那是确定性容差定义问题（ADR-003/005 范畴），由 Andy 单独起草 ADR-005 容差复现补丁处理（run-to-run ≤1 LSB 感知底噪 = 可接受，bit-exact 非基准）。请勿与本实验的 AI 判定混为一谈。

## 七、非目标

自由粒子编辑器 / 新模板 / 改确定性引擎的运镜或合成核心逻辑 / 放松既有视觉门 / 无判据地永久启用 video2video。（本实验只在 §五 路由判定后，才决定 video2video 或回退线的正式去留；任一去留都是启鸣拍板 + Andy 起草对应 ADR。）
