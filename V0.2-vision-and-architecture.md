# CineAnchor V0.2 — 大方向与架构（Vision & Architecture）

> 作者：Andy（产品收敛）· 待启鸣验收
> 状态：草案 · 日期：2026-07-02
> 前置：V0.1（loops 01-09）已完成渲染链路 + 确定性镜头引擎

---

## 一、一句话定位

把 CineAnchor 从「能渲染单个主体和镜头」的**技术 Demo**，升级成「填模板就能稳定产出可发布的竖屏游戏运营视频」的**产品 MVP**。

用户流程：**选模板 → 填素材 → 填文案 → 选主题色 → 导出视频**。

## 二、V0.1 已有什么、V0.2 补什么

**V0.1 已打通（不重做）：**
- Web → FastAPI → Project JSON → Blender Eevee → FFmpeg → MP4 全链路
- PNG / GLB 上传；`character_intro` / `product_orbit` 两个基础场景
- **确定性镜头引擎**（loops 07-09）：Catmull-Rom + SLERP 轨迹、8 预设、交互式录制、preview↔render parity（oracle 守护）

**V0.2 要补的短板：** 没有模板化生产、没有面向运营用户的填表 UI、字体样式未接入、素材缺失时不稳。

## 三、核心产品原则（不可动摇）

**模板化，不是自由编辑器。** 用户**不面对**：自由摆多主体 / 手动镜头 / 手动 FOV / 复杂 Prompt / 几何体控制 / 时间线剪辑。用户**只面对**：模板 + 素材 + 文案 + 主题色。

> 底线：不做成低配剪映 / 低配 Blender / AI 视频实验版。做成**模板化视频生产 MVP**。

## 四、架构决策（V0.2 的关键，请重点验收）

### 4.1 镜头引擎「沉到模板内部」——护城河作为引擎保留，作为 UX 隐藏

loops 07-09 的确定性镜头引擎**不废弃**，它成为**模板内部的运动系统**：模板用 preset/keyframe 生成「角色海报页缓推」「枪械光扫环绕」这类镜头运动，**用户看不到也不碰任何镜头参数**。这是 V0.2 与前三个 loop 的衔接点，不是推翻。

### 4.2 保持确定性，V0.2 不引入新 AI

- 粒子（花瓣/光点/火星/冰晶）= 走 **ADR-006 确定性方法**。
- AI spark（ADR-007）保持**手动 / 可选**，**不进模板自动链路**。
- **V0.2 渲染链纯确定性，无新增 AI 依赖。** 理由：AI 破坏角色/文字/帧间一致的风险，正是四条铁律要防的。

### 4.3 素材前提：透明 PNG，自动抠图明确后置

- V0.2 **要求用户上传已抠好的透明背景 PNG**；系统**不做 auto-matting**。
- 自动抠图 = 明确后置（V0.2+）。带背景图导致视觉崩，先用约束挡住。

### 4.4 Project JSON schema 向后兼容扩展

- 新增模板化素材 + 时间轴结构，**绝不破坏** `character_intro` / `product_orbit`。
- JSON 仍是唯一真相；模板是 JSON 的**生成器**，渲染后端不为模板改动。

### 4.5 预览架构：继续 Three.js（loop 09 spike 已定）

- loop 09 Blender-native-preview spike 结论 = **不切，保 Three.js**。V0.2 预览沿用 Three.js，不重开架构问题。

## 五、组件库：先具体两次，再抽象一次（反投机抽象）

**不预先设计通用组件库。** 正确顺序：
1. 先把**生日模板**具体地做出来（哪怕逻辑写死）。
2. 做**枪皮模板**时，真正共享的形状会浮现。
3. **那时才抽**共享组件（Date Card / Title Ribbon / Subtitle / Particle Overlay / Logo End Card / Light Sweep 等）。

现在就设计 10 个组件的通用接口 = 大概率抽错、返工。组件库列表作为**候选清单**记录，不作为 V0.2 前期交付。

## 六、质量闸门（延续铁律）

- **视觉闸门照旧、且强制**，尤其**文字定位**（字卡/字幕/日期卡）——这是 loop 07/08 让我们流血的 PERSP×title 老问题的放大版，绝不能只靠单测绿。
- 缺可选素材（无 BGM / 无实机片段 / 无补充图）**不崩**，仍可导出。
- 旧模板零回归。

## 七、交付范围与优先级

| # | 交付物 | 优先级 | 形态 |
|---|--------|-------|------|
| ① | 角色生日模板（15s MVP 先行） | **P0** | 完整实现（loop 10） |
| ② | 枪械皮肤展示模板 | P1 | 可先设计文档 + 最小 PNG 版（loop 11） |
| ③ | reference_video_analysis（真实竞品反推） | **P0 前置** | 设计文档 |
| ④ | Project JSON schema 扩展草案 | P0 | 产品决策，Andy 收敛 |
| ⑤ | 组件抽象 | P2（第二模板后） | 见 §五 |
| ⑥ | Web UI 改填表式 | P1 | 生日模板可用后接入 |

## 八、流程说明

- **V0.2 = 里程碑横幅**，底下继续用现有 loop 流程和编号：loop 10 = 生日模板 MVP，loop 11 = 枪皮模板，依此类推。不另起一套流程。
- 需求/验收 = 中文（启鸣决策）；plan/ADR/tech = 英文。
- ADR 在**每份 requirements 验收后**由 Andy 起草（ADR 不需启鸣验收）。V0.2 预计触发的 ADR：模板化架构（镜头引擎内化 + JSON schema 扩展 + 确定性边界）。

## 九、V0.2 最小完成标准（对齐启鸣草案）

1. 生日模板完整可用（15s 起）；2. 可传角色 PNG + Logo；3. 可输入角色名/日期/标题/字幕；4. 输出 9:16 1080×1920 MP4；5. 含日期卡/海报/字幕/粒子/片尾；6. Web UI 全流程可操作；7. 旧模板不坏；8. 有测试；9. 有文档；10. 有一条可展示样例视频。

## 十、明确不做（V0.2 边界）

复杂 AI 视频生成 / 自由多主体 3D 编辑 / 复杂角色动作 / 用户手动剪辑时间线 / 高自由 Prompt / 暴露 FOV 镜头控制 / 自动抠图 / 项目历史 / 账号 / 全功能实时预览 / 批量生成。（BGM、GLB 高级展示 = 可后置。）
