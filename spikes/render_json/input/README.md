# Route 2 Input Assets

Runtime assets in this folder are intentionally ignored by Git.

- `hero.png`: copied from the accepted Route 1 PNG spike input.
- `model.glb`: copied from the accepted Route 0 GLB spike input.

The Project JSON files reference these assets by relative path so the renderer does not hardcode local absolute paths.
